from collections import deque

class Queuedeque:
    def __init__(self):
        self.queue = deque()

    def enqueue(self, item):
        self.items.append(item):

    def dequeue(self):
        if len(self.items) < 1:
            return None
        return self.item.popleft() + Faster than pop (0)

    def seize(self):
        return len(self.items)